# Phase 1-2 — docs -> ApiSchema -> generated fetch script
# Python 3.11+.  Install:  pip install -r requirements.txt

# --- Doc fetching ---
requests
beautifulsoup4
lxml
PyMuPDF

# --- RAG: chunking, embeddings, vector store (LangChain stack) ---
langchain-text-splitters
langchain-huggingface
langchain-chroma
langchain-core
sentence-transformers
chromadb

# --- LLMs ---
google-genai             # primary (Gemini)
groq                     # fallback (Groq, OpenAI-compatible)

# --- Validation & config ---
pydantic
python-dotenv

# --- Testing ---
pytest