# Phase 1 — Docs → api_schema (RAG over API documentation)
# Python 3.11+.  Install:  pip install -r requirements.txt

# --- Doc fetching ---
requests
beautifulsoup4
lxml
PyMuPDF                  # PDF parsing (import as: fitz)

# --- RAG: chunking, embeddings, vector store (LangChain stack, matches MDIS) ---
langchain-text-splitters
langchain-huggingface
langchain-chroma
langchain-core
sentence-transformers    # BGE-small backend (pulled by langchain-huggingface; pinned for clarity)
chromadb                 # pulled by langchain-chroma; pinned for clarity

# --- Structured extraction (LLM, next batch) ---
google-genai             # unified Gemini SDK (NOT the deprecated google-generativeai)

# --- Validation & config ---
pydantic
python-dotenv

# --- Testing ---
pytest